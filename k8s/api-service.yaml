apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: syedshoib-dev

spec:
  selector:
    app: api

  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP

  type: ClusterIP
